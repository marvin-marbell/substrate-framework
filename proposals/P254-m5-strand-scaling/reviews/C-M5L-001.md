# C-M5L-001: constructive mathematical and governance review

## Claim and Useful Role

The proposed claim is an exact **conditional** straight-strand radial infimum with explicit `m=2k`, fixed core/exterior and other eigenvalues, separate positive static contractions and distinct spectral/trace potentials. Its useful role is to discriminate the reported beta³ and delta⁴ powers without conflating the actions, to correct smooth-attainment language at full winding, and to prevent using a frozen-slot line value as a full-field lower bound or a confinement observation.

## Frozen Transaction

Base `substrate-framework@9032348018fd03c921d819dc660e85587a832bce`, release v0.183.0. Primary external source pins and already-exposed scripts are in `../proposal.yaml` and `../thread-harvest.md`; no accepted-claim dependency licenses the source action. Implementation `src/substrate_framework/m5_strand.py`, independent-oracle regression `tests/test_m5_strand.py`, proof `../proof.md`; reviewer saw the worktree before promotion and was instructed to perform read-only mathematics or governance inspection without running tests. Initial math review `agent://StrandMathReview`; governance review `agent://StrandGovernanceReview`. The claim was **not accepted on initial review** because a full-field-gradient statement and manifest normalization were wrong.

## Strongest Supported Statement

On the radial fixed-slot chart, `A=16` for Report 018 and `A=32` for R25, so the sharp finite-energy radial infima are `sqrt(2) pi |m| beta³/3` and `2 pi sqrt(32w)|m| integral_0^b0² (b0²−q)sqrt(K(q))dq` respectively. At `s0=b0=delta/2` the second is `pi sqrt(2w)|m| delta⁴/2 [1+9delta²/8+O(delta⁴)]`. Its constant-`K_leading` bound is strict for positive splitting. For `|m|>1` the BPS profile is non-C1 at the core; smooth approximants recover the same **infimum** at `O(epsilon²)` cost, but no smooth equality minimizer exists. The larger R25 static chart has an explicit lower-energy director-slot competitor at identical curvature cost; the radial number is **not** a full static lower bound. None of these results establishes a localized electron, physical string between pair charges, or Newton force.

## Evidence Map

| Evidence | Proposition and role | Bridge and limit |
| --- | --- | --- |
| `../proof.md` matrix and square argument | Exact proof on explicitly restricted radial class | Endpoint and regularity are hypotheses; not full 4D theorem |
| `src/substrate_framework/m5_strand.py` + `tests/test_m5_strand.py` | Symbolic matrix/trace oracles, negative director variation, axis and stiffness countercases; regression | Test evaluator is independent of author-script bound; not continuum pair-field simulation |
| Report 018 `strand_bogomolny.py` at 4823e5e | Source-matrix identity True and printed bound; provenance/corroboration | Author code does not check smooth core or full pair energy |
| OpenWave `m5_32_r25_0_form.py run` at a73f6fc | Author groups a,a2,b,d,e,f PASS; provenance/corroboration | Pinned 1D results do not promote unconverged 3D lattice |
| Report 017 `symbolic.py` at 4823e5e | Positive field-norm kinetic/form identity; compatibility warning | Different contraction, no full R25 hyperbolicity |
| `agent://StrandMathReview` | Independent approach audit identified a falsified gradient-cost assertion and exact negative director first variation | Review initially returned incorrect; corrections require a scoped check |
| `agent://StrandGovernanceReview` | Independent contract audit found `m` versus `k` factor-four kinetic mismatch, action-specific exterior ambiguity, wrong campaign target kind | Initial manifest needed correction; no merge authorization |

## Oracle and Approach Audit

Tests derive `F_xy` from independent 2x2 matrix derivatives and derive all four trace differences from an independent mixed 4x4 matrix. Halving the 2Tr coefficient leaves exact nonzero residual `32 b²(b')²/rho²`; releasing the regular axis to `q(0)=1/4` reduces an example topological integral to `9/16` of its pinned value; replacing exact variable K by frozen K at delta=.3 underestimates the infimum by factor `1.0036907831480166`. The independent positive director variation and curvature commutation test rejects treating the fixed-slot bound as full-field. These wrong cases reach the tested assertions; no claim is based on a test count or a source script printing its own bound.

## Minimum Corrections and Next Achievements

The math reviewer observed that `proof.md` had mistakenly warned of a gradient penalty from a free director slot. Correction: in R25 the 33 diagonal gradient commutes with the pair curvature and `dV/dd|_1=2w(q−q0)[4+18s0+4(12s0²+2(q+q0))]<0` on an annulus, yielding a genuine lower-energy static competitor. The corrected proof, API and independent matrix regression agree; `agent://StrandMathReview` checked the exact changed edges and reported no remaining finding (read-only, no test run). The governance reviewer observed that the manifest used `Ak²` although the source density is `Am²=4Ak²`, conflated beta and delta endpoints, and classified its single route as mechanism selection. Corrections use `m=2k`, distinct exterior amplitudes and `target_kind: fixed_theorem`; `agent://StrandGovernanceReview` confirmed all three corrected edges and truthful active status (read-only, no test run). Stronger full-theory or pair claims require their own finite-energy/free-boundary solver and action-specific comparison.

## Compatibility and Four-Axis Decision

Verification is **symbolic_verified** for the fixed-slot theorem and constructive director counterexample, with numerical quadrature used only to corroborate a recovery sequence. Independent mathematical and governance reviews found errors in the initial draft, and each then approved its corrected scoped edges; review decision for exactly C-M5L-001 is **accepted** on those conditions, not a general review of the thread. Compatibility is **compatible_extension**: the claim does not replace the accepted C-M5C/C-M5W auxiliary-frame action or equate the two external actions. Epistemic status is **active conditional theorem** when the matching registry/release/generated transaction completes; the review alone does not confer publication or merge authority.

## Promotion and Correction Check

The claim registry, source API and tests, terminal P254 manifest, immutable release v0.184.0/current pointer, generated claim index and accepted framework claim/release memory are the promotion transaction. The two scoped correction checks are recorded above; reviewer approval is separate from the author-owned validations and the distinct GitHub merger. Issue #218 and its review PR stay open for a separate reviewer; #211 stays open for unresolved broader discussion questions.

## Result and Frontier

The exact radial proof plus a full-static-field counterexample is a useful bounded result, not an electron or confinement claim. Reproduction is `PYTHONPATH=src .venv/bin/python -m pytest tests/test_m5_strand.py -q`. The next physical achievement is a same-action finite-energy free-field strand/charge and matched two-body force control with boundary, core, residual and topology checks; no such result is claimed here.
