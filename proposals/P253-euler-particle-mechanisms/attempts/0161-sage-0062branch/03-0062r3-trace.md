# 03 — 0062-R3: sandwiched trace at lambda_* (bankable round 3)

Scope: 00-scope.md (frozen, this attempt), round R3 per the frozen
plan, authorized (#113 + acting-shepherd routing). Consumed: R2's
localized two-mode block (run_0062r2 banked: exact lambda_pm,
channel-open coupling form); 0058 nondivisibility anchor; R1 seed.
THE REGISTERED OBLIGATION FROM #113 IS PAID IN THIS ROUND (RR3-4).
Receipts: receipts/run_0062r3.py + run_0062r3.log (5 assertions —
4 identity + 1 mutation — exit 0). Discipline: model-level
identities only; the continuum limiting-absorption trace theorem
stays governed by the 0062 README and is NOT claimed.

## Constructed structure (each step receipt-backed)

- **RR3-1 (distorted adjoint, EXACT):** at λ = λ_* the resonant
  left state **ψ_* = (g, λ_* − D₁)** satisfies M(λ_*)ᵀψ_* = 0
  EXACTLY (both rows annihilated on the inner-branch root via the
  det = 0 relation), nonzero for g ≠ 0 — the resonant left state
  exists at the localized λ_*.
- **RR3-2 (residue normalization (10)):** the pairing
  ⟨ψ_*, ∂_λM(λ_*)ψ_*⟩ = −|ψ_*|² is REAL and NONZERO (exact);
  the normalization constant c = |ψ_*|^{−1} sets the residue to
  unit modulus. The SIGN of the pairing is recorded and the
  KKS/energy sign convention travels to R4 as a labeled tier
  (model-level scaling receipt; the physical residue sign is R4's
  object).
- **RR3-3 (range condition (21); mutation):** the distorted
  functional ⟨ψ_*, N⟩ and the raw component pick N(I_*) are
  DIFFERENT linear objects: on the generic test amplitude
  N = (λ_* − D₁, −g) the distorted functional passes (= 0) while
  the raw pick is generally nonzero (witness D₁ = 3, D₂ = 1,
  g = 2: raw = −1 − √5). Replacing (21) by raw N(I_*) is caught
  (0058 nondivisibility class, now at trace level).
- **RR3-4 (REGISTERED OBLIGATION #113 PAID):** the coupling's
  nonvanishing AT λ_* is exhibited at model scope: witness
  (D₁ = D₂ = 1, g = 2) → λ_* = 3 with ψ_* = (2, 2) ≠ 0 and
  g = 2 ≠ 0 AT the resonance. g is λ-independent (frozen-covector
  Hodge element), so its nonvanishing at λ_* reduces to the
  CHANNEL-OPEN condition (ω₀·k ≠ 0, #113 form). The continuum
  exhibition rides R4's physical normalization (labeled tier).
- **RR3-5 (finite rows, model inventory):** the circulation row
  DECOUPLES for n ≠ 0 — the zero-mode projection of e^{inθ} over
  [0, 2π] vanishes exactly (no net circulation in a nonzero
  toroidal harmonic); the interface continuity row is carried by
  the R1 no-sheet structure (MB1-2) and the exterior row by the
  global kernel — the trace's row inventory is FINITE by
  construction at two-mode scope (2×2 + named rows).

## R3 verdicts (PROPOSED — drift review requested)

- **R3-trace:** the sandwiched trace structure EXISTS at two-mode
  model scope: distorted adjoint ψ_* with exact null-state
  identity, real nonzero residue with unit-modulus normalization,
  distorted range condition distinct from the raw pick (mutation
  live), finite named rows, and the registered #113 obligation
  (g ≠ 0 at λ_*) exhibited. **NO kill fired; no STOP rule
  triggered** (single fixed-n block; all inputs at stated scopes).
- **What this round does NOT claim:** the continuum
  limiting-absorption theorem and the physical (continuum) trace —
  function-space statements are governed by the 0062 README and
  the consumed 0159/0052 scopes; the residue's physical SIGN and
  normalization convention are R4's objects (labeled tier);
  V_* is R4's object.
- Scope deltas: none. Fences restated: P2/LP2 classical only; no
  carrier/electron/quantum claim; no measurement; append-only;
  drift firewall reviews every round.

## Next

R4 V_* EVALUATION per the frozen plan: the recursively generated
resonant coefficient (24) from the Cao profile, exact Green/Leray
block, lower recurrences, physical measure — sign, dimensions,
delta-scaling, analytic remainder; the README three-outcome fork
recorded; the KKS sign convention lands here (labeled tier from
RR3-2). Then R5 (branch decision).
