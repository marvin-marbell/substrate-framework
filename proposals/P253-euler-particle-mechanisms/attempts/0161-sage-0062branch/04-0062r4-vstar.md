# 04 — 0062-R4: V_* model evaluation (bankable round 4)

Scope: 00-scope.md (frozen, this attempt), round R4 per the frozen
plan, authorized acting-shepherd routing (the load-bearing build;
the KKS sign-convention tier rides with it — RR3-2's recorded sign
lands here). Consumed: R3 trace (ψ_* null state, residue
normalization), R2 localized λ_pm (channel-open coupling), R1 seed,
0005 KKS convention, README (22)/(24) + three-outcome fork.
Receipts: receipts/run_0062r4.py + run_0062r4.log (8 assertions —
6 identity + 2 mutations — exit 0). Discipline: model-level
identities only; the continuum V_* theorem and physical units stay
governed by the 0062 README and are NOT claimed.

## Constructed structure (each step receipt-backed)

- **RR4-1 (harmonic bookkeeping, EXACT):** e^{inθ}e^{inθ} = e^{2inθ}
  (nonresonant for n ≥ 1); e^{inθ}e^{−inθ} = 1 (resonant zero
  frequency) — the zero-frequency part of the order-2 residual
  arises ONLY from the (+n, −n) cross pair.
- **RR4-2 (j_* = 2 at model scope):** NO order-1 resonance exists
  (a single harmonic never reaches zero frequency); the order-2
  cross pair is exactly resonant — the first source-bearing order
  at two-mode scope is j_* = 2. Labeled tier: the source-defined
  j_* of (23) is Θ((δ²log(1/δ))⁻¹) in the full carrier; the model
  statement is the bookkeeping-exact quadratic term per the README
  fork wording.
- **RR4-3a (PRINCIPAL-ORDER TRANSPARENCY — structural finding):**
  at the transverse witness (k = (1,1,1), e₁ = (1,−1,0),
  e₂ = (1,1,−2), mutually perpendicular) the frozen transverse
  bracket for the back-to-back pair VANISHES IDENTICALLY —
  transversality kills both advective pieces. The resonant coupling
  is carried ENTIRELY by the K-channel (README: K is not a
  discarded lower-order symbol) — the model echo of the 0058
  polarization split; it sharpens the localization: transparency at
  principal order, coupling through K.
- **RR4-3b (V_* model assembly through the K-channel):**
  **V_*^{model} = g·(ω₀·k)/|k|² · ⟨e₁, k×e₂⟩ · c_ψ** FACTORIZES
  through the three independently receipted pieces — coupling
  g = 2 (RR2-3/RR3-4, channel-open), ω₀·k = 3 (open channel),
  angular ⟨e₁, k×e₂⟩ = −6 (RR4-4): **V_model = −12 ≠ 0 EXACTLY**
  at the witness. Nonvanishing under exactly the #113 open
  conditions; the sign is locked to the KKS area tier.
- **RR4-4 (KKS sign tier lands):** the transverse area pairing
  ⟨e₁, k×e₂⟩ = −6 receipted relative to RR3-2's recorded residue
  sign (pairing = −|ψ_*|² < 0): the 0005 convention
  Ω = −⟨m,[ξ,χ]⟩ at model level; the normalized pair's orientation
  is k-orientation-locked; the ABSOLUTE physical sign remains a
  labeled tier (R4-continuum/R5).
- **RR4-5 (dimensions, model bookkeeping):** the assembled V_model
  is a pure number at witness scope; the physical 1/T² composition
  ([ψ][B^][bracket], k absorbed in the declared ξ) attaches only
  with the declared constants K, ξ, p and the R4-continuum
  normalization (labeled tier, stated not tested).

## Mutations (negative controls — the kills are live)

- **MB4-1 (self-interaction substitution):** dropping the (n,−n)
  cross pair makes the resonant projection VANISH exactly
  (V_self = 0 at the same witness) while V_model ≠ 0 — a "direct
  quadratic self-interaction" substitution for the recursive (24)
  is caught (README oracle); the resonant content lives in the
  cross pair.
- **MB4-2 (fitted-scale substitution):** rescaling the coupling by
  the universal filament compression Λ_fil/3 changes the
  coefficient — the declared-kernel constant set is not
  substitutable (README oracle).

## R4 verdicts (PROPOSED — drift review requested)

- **R4-V\* (model tier):** the resonant coefficient is ASSEMBLED at
  two-mode model scope and FACTORIZES through the three receipted
  pieces; V_model = −12 ≠ 0 at the open-channel witness — the
  README three-outcome fork resolves at model scope to outcome 1
  (V ≠ 0 activates the nonlinear inner layer), CONDITIONAL on the
  channel-open form (#113) and the continuum trace tiers.
  **Structural finding banked alongside:** principal-order
  transparency for transverse pairs (RR4-3a) — the coupling is a
  K-channel effect, which is precisely why the block-level
  localization (R2) and the transparency fork are nontrivial.
- **What this round does NOT claim:** the continuum V_* (with its
  analytic remainder and δ-scaling on the actual carrier — those
  ride the R3-continuum trace and the physical normalization);
  the absolute physical sign (KKS tier, labeled); nonlinear
  convergence (R5's object). The three-outcome fork statement is
  MODEL-scope.
- Scope deltas: none. Fences restated: P2/LP2 classical only; no
  carrier/electron/quantum claim; no measurement; no fitted scales
  (MB4-2 live); append-only; drift firewall reviews every round.

## Next

R5 BRANCH DECISION per the frozen plan: the conjugacy-topology
partition FIRST (F′(ζ) critical sets, Reeb graph, ζ-critical
hyperbolic orbit with KKS orientation, boundary/exterior
continuation), then per partition 1/2/3; outcome-1 activation means
the inner-layer construction is the licensed continuation with the
RR4-3a transparency constraint carried (the layer is a K-channel
effect, not a principal-order transport effect).
