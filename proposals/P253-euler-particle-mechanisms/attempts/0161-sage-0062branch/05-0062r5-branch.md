# 05 — 0062-R5: branch decision RECEIPTED (lane-closing round 5)

Scope: 00-scope.md (frozen, this attempt), round R5 per the frozen
plan, authorized acting-shepherd routing (the lane-closer). THE
CONJUGACY-TOPOLOGY PARTITION COMES FIRST; the RR4-3a constraint
travels: any layer is a K-channel effect, not a principal-order
transport effect. Consumed: 0062-established centralizer topology
(0063-corrected); RR4-3a transparency constraint; RR4-4 KKS area
sign; RR3-2 residue sign. Receipts: receipts/run_0062r5.py +
run_0062r5.log (6 assertions — 3 identity + 2 mutations +
1 structure — exit 0). Discipline: model-level identities only;
the smooth matching (partition-2 execution) and the continuum
branch are the named next constructions (owner charter), NOT
claimed by this lane.

## Receipted partition procedure (each step exact)

- **RR5-1 (regular-core gate):** the centralizer flow magnitude on
  a ζ-torus is Y = F′(ζ)·c: **|Y| = 0 ⟺ F′(ζ) = 0**. At a REGULAR
  trace (F′ ≠ 0, ∇ζ ≠ 0) the flow has no fixed points on the torus
  (flow-box) — no separatrix can form: **partition = transparency-
  at-trace or advance; a layer is FORBIDDEN by smooth conjugacy**
  (the 0062-established prohibition, now gated by an exact
  receipt).
- **RR5-2 (F′ = 0 ⇒ degenerate whole level):** with model
  cross-section ζ = r² − z² and F′ = ζ, the flow-magnitude zero
  set is {ζ = 0} = {r² = z²} — containing (0,0), (1,1) AND (2,2):
  a full level through the critical point, NOT an isolated point.
  The README test question is ANSWERED: F′(ζ) = 0 creates only a
  degenerate whole level; isolated-saddle formation from F′ alone
  is refuted at model level.
- **RR5-3 (ζ-critical hyperbolic orbit):** the centralizer
  linearization at a nondegenerate ζ-critical point:
  Y_lin = F′(ζ₀)·J·H·(x − x₀) with J = [[0,−1],[1,0]] has
  charpoly λ² + h₁h₂ — eigenvalues ±√(−h₁h₂): **REAL and opposite
  for INDEFINITE H** (witness diag(1,−1) → {+1,−1}: an ISOLATED
  hyperbolic orbit EXISTS — the licensed partition-2 path);
  **purely imaginary for DEFINITE H** (witness I → {±i}: center,
  no separatrix from this path). The partition flips exactly on
  the Hessian signature.
- **RR5-4 (KKS orientation chain, structure):** one consistent sign
  bookkeeping from the resonant pairing (RR3-2: −|ψ\*|² < 0)
  through the KKS area (RR4-4: −6 < 0) to the orbit orientation =
  sign(F′(ζ₀)) against that chain; the absolute physical sign
  remains the labeled tier.

## Mutations (negative controls — the kills are live)

- **MB5-1 (partition violation):** a singular-layer (partition-3)
  claim AT a regular-core trace is caught — at F′ ≠ 0 the flow is
  nonzero everywhere on the torus (flow-box) while the pendulum
  separatrix needs a hyperbolic fixed orbit.
- **MB5-2 (isolated-saddle misread):** reading F′ = 0 as an
  isolated saddle contradicts the full-level zero set — the
  degenerate-whole-level reading is the receipted one.

## R5 verdicts (PROPOSED — drift review requested)

- **R5-branch: THE PARTITION PROCEDURE IS RECEIPTED.** The branch
  decision at fixed toroidal n is now an exact procedure over the
  resonance location:
  (a) regular-core resonance (F′ ≠ 0, ζ noncritical) →
      **partition 1**: transparency chains; the layer is forbidden
      by smooth conjugacy;
  (b) ζ nondegenerate-critical resonance with INDEFINITE Hessian →
      **partition 2 LICENSED**: the smooth compatible-centralizer
      path carries the required hyperbolic orbit with the receipted
      KKS orientation — the smooth branch construction is the
      licensed continuation;
  (c) degenerate levels or definite Hessian → NO smooth path:
      **partition 3** only as a separately licensed weaker route
      (outside the frozen smooth contract).
  WITH RR4-3b (V ≠ 0, channel-open) the outcome-1 activation
  carries the RR4-3a constraint: the layer, if constructed, enters
  through K — never through the principal transport.
- **Lane-closer statement (0161 model-scope lane):** R1 seed → R2
  resonance localization → R3 sandwiched trace (distorted adjoint,
  residue, finite rows, range condition) → R4 V\* model evaluation
  (factorized, nonvanishing, KKS tier) → R5 partition procedure.
  **NO kill fired across all five rounds; no STOP rule triggered.**
  Every round: doc + receipts + mutations + commit; drift reviewed
  R1–R3 (R4/R5 review requested); scope deltas: none beyond D1's
  (0160) — the 0161 rounds ran inside the frozen scope.
- **Named next constructions (NOT claimed by this lane):** the
  partition-2 EXECUTION (the smooth compatible-centralizer matching
  and separatrix solve on the actual carrier — continuum scope,
  owner charter); the continuum limiting-absorption trace (R3
  tier); the physical residue normalization (R4 tier). FB-D-waves
  fireability route (independent-p protocol) remains the FB-D
  named-open.
- Fences restated: P2/LP2 classical only; no carrier/electron/
  quantum claim; no measurement; no fitted scales; no LANE-1
  membership; no nonlinear-stability claim (separate P2 rung).
